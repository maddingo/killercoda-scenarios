# Preparing the environment

In this tutorial we use [SDKman](https://sdkman.io). It should already be installed. Check with `sdk version`{{execute}}.

If not, it can be installed with `curl -s "https://get.sdkman.io" | bash`{{execute}}.


